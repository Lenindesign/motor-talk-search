
import React from 'react';
import StatsVariationSelector from './garage-stats-variations/StatsVariationSelector';

// Temporarily replace the original component with the variation selector
const GarageStats = () => {
  return <StatsVariationSelector />;
};

export default GarageStats;
